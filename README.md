# 📚 Edu Platform — учебная платформа на Spring Boot, JPA и PostgreSQL

Проект представляет собой учебную платформу с поддержкой пользователей, курсов, модулей, уроков, заданий, решений и тестов.
Система реализует многослойную архитектуру, сложную модель данных с множеством связей, а также REST API для всех основных операций.

Проект создан в рамках практики по работе с ORM (JPA/Hibernate) и соответствует требованиям учебного ТЗ.

---

# 🧩 Стек технологий

- Java 17
- Spring Boot 3
- Spring Data JPA / Hibernate
- PostgreSQL
- Maven
- Testcontainers
- Docker / docker-compose
- Jakarta Validation
- Lombok

---

# 🗄 Архитектура проекта

```
src/main/java/com/example/eduplatform
 ├── controller     — REST-контроллеры, DTO, валидация
 ├── service        — бизнес-логика
 ├── repo           — Spring Data JPA репозитории
 ├── domain         — JPA-сущности (Lazy loading)
 ├── exception      — глобальная обработка ошибок
 └── EduPlatformApplication.java
```

---

# 🧮 Модель данных

Модель включает ключевые сущности учебной платформы:

- User
- Course
- Module
- Lesson
- Assignment
- Submission
- Enrollment
- Quiz
- Question
- AnswerOption
- QuizSubmission

Связи:
- 1‑ко‑многим: Course → Module, Module → Lesson, Lesson → Assignment, Quiz → Question → AnswerOption
- Многие‑к‑многим: User ↔ Course через Enrollment
- Lazy Loading используется для всех коллекций

---

# ⚙️ Запуск проекта

## 1. Запуск PostgreSQL
```
docker compose up -d
```

## 2. Запуск приложения
```
mvn spring-boot:run
```

### Переменные окружения
```
DB_HOST=localhost
DB_PORT=5432
DB_NAME=edu
DB_USER=edu
DB_PASSWORD=edu
```

---

# 📑 Основные REST API

## Пользователи
- POST /api/users
- GET /api/users

## Курсы
- POST /api/courses
- GET /api/courses

## Запись на курс
- POST /api/enrollments

## Задания и решения
- POST /api/assignments
- POST /api/submissions
- PUT /api/submissions/{id}/grade

## Тесты
- POST /api/quizzes
- POST /api/quizzes/{id}/questions
- POST /api/questions/{id}/options
- POST /api/quizzes/{id}/submit

---

# 🧪 Тестирование

```
mvn test
```

Покрытие тестов:
```
target/site/jacoco/index.html
```

---

# 🐳 Docker

```
docker compose up --build
```

---

# 🛠 Обработка ошибок

Используется глобальный обработчик исключений (ControllerAdvice).

Ошибки возвращаются в формате JSON:
```
{
  "timestamp": "...",
  "message": "Entity not found",
  "path": "/api/courses/99"
}
```

---

# ✔ Соответствие ТЗ

Проект реализует основные требования:
- ORM‑модель с множеством сущностей
- Lazy Loading
- CRUD для всех основных сущностей
- Управление курсами, модулями, уроками
- Запись студентов на курсы
- Работа с заданиями и решениями
- Система тестирования
- PostgreSQL + конфигурация через env
- Интеграционные тесты (Testcontainers)
- REST API
- Докеризация

---

# 📄 Лицензия

Лицензия не указана (можно добавить MIT/Apache 2.0 при необходимости).

